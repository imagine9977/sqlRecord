<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sqlrecord.sqlrecord.orders.model.dao.OrdersMapper">

	
	<insert id="insertMemberOrders">
		INSERT
		INTO
				MEMBER_ORDERS
		VALUES
				(
				MEMBER_ORDERS_SEQ.NEXTVAL,
				#{member_orders_address},
				#{member_orders_address2},
				#{member_orders_postcode},
				SYSDATE,
				#{member_no}
				)
	</insert>

	<select id="selectOneMemberOrdersNo" parameterType="_int" resultType="_int">
		SELECT
				Max(MEMBER_ORDERS_NO)
		FROM
				MEMBER_ORDERS
		WHERE
				MEMBER_NO = #{member_no}
		ORDER
		BY
				MEMBER_ORDERS_NO
		DESC
				
	</select>
	
	
	<insert id="insertOrdersDetail" parameterType="OrdersDetail">

		INSERT
		INTO
				MEMBER_ORDERS_DETAIL
		VALUES
				(
				MEMBER_ORDERS_DETAIL_SEQ.NEXTVAL,
				#{product.product_no},
				#{memberOrders.member_orders_no},
				#{member_orders_detail_amount},
				#{member_orders_detail_price},
				DEFAULT,
				DEFAULT,
				DEFAULT
				)

	</insert>
	
	<resultMap type="OrdersDetail" id="odRM">
    	<id property="member_orders_detail_no" column="MEMBER_orders_detail_no"/>
    	<result property="member_orders_detail_amount" column="MEMBER_orders_detail_amount"/>  
	    <result property="member_orders_detail_price" column="MEMBER_orders_detail_price"/>
	    <result property="tracking_num" column="TRACKING_NUM"/>
	    <result property="member_orders_detail_exd" column="MEMBER_ORDERS_DETAIL_EXD"/>
	    <result property="member_orders_detail_status" column="MEMBER_ORDERS_DETAIL_STATUS"/>
	    <association property="product" javaType="product">
	        <result property="product_no" column="product_no"/>
	        <result property="product_name" column="product_name"/>
	        <result property="product_price" column="product_price"/>
	        <result property="product_status" column="product_status"/>
	        <result property="product_date" column="product_date"/>
	        <result property="tag_no" column="tag_no"/>
	        <result property="product_detail" column="product_detail"/>
	        <result property="product_cate" column="product_cate"/>
	        <result property="color" column="color"/>
	        <association property="product_photos" javaType="product_photos">
	        	<result property="photo_no" column="PHOTO_NO"/>
			    <result property="product_no" column="PRODUCT_NO"/>
			    <result property="photo_name" column="PHOTO_NAME"/>
			    <result property="photo_path" column="PHOTO_PATH"/>
	        </association>
	    </association>
	    <association property="memberOrders" javaType="memberOrders">
	    	<result property="member_orders_no" column="MEMBER_ORDERS_NO"/>
	        <result property="member_orders_address" column="MEMBER_ORDERS_ADDRESS"/>
	        <result property="member_orders_address2" column="MEMBER_ORDERS_ADDRESS2"/>
	        <result property="member_orders_postcode" column="MEMBER_ORDERS_POSTCODE"/>
	        <result property="member_orders_date" column="MEMBER_ORDERS_DATE"/>
	        <result property="member_no" column="MEMBER_NO"/>
	    </association>
	</resultMap>
	
	
	<select id="getOrdersDetail" resultMap="odRM">
	    WITH RANKEDMOD AS (SELECT
		        od.MEMBER_ORDERS_DETAIL_NO,
		        p.PRODUCT_NO,
                p.PRODUCT_NAME,
                p.product_cate,
                p.product_price,
                p.product_status,
                mo.MEMBER_NO,
                mo.MEMBER_ORDERS_ADDRESS,
                mo.MEMBER_ORDERS_ADDRESS2,
                mo.MEMBER_ORDERS_POSTCODE,
                mo.MEMBER_ORDERS_DATE,
		        od.MEMBER_ORDERS_NO,
		        od.MEMBER_ORDERS_DETAIL_AMOUNT,
		        od.MEMBER_ORDERS_DETAIL_PRICE,
		        od.MEMBER_ORDERS_EXD,
		        od.TRACKING_NUM,
		        od.MEMBER_ORDERS_DETAIL_STATUS,
                ps.photo_path,
                ROW_NUMBER() OVER (PARTITION BY p.PRODUCT_NO ORDER BY od.MEMBER_ORDERS_NO DESC) AS rn
                
	    FROM
	       	 	MEMBER_ORDERS_DETAIL od
        JOIN
                MEMBER_ORDERS mo
        ON
                od.MEMBER_ORDERS_NO = mo.MEMBER_ORDERS_NO
        JOIN
                PRODUCT p
        ON
                od.PRODUCT_NO = p.product_no
        LEFT JOIN
	        PRODUCT_PHOTOS ps ON p.PRODUCT_NO = ps.PRODUCT_NO
	   WHERE
                mo.MEMBER_NO = #{member_no})
        SELECT
                MEMBER_ORDERS_DETAIL_NO,
		        PRODUCT_NO,
                PRODUCT_NAME,
                product_cate,
                product_price,
                product_status,
                MEMBER_NO,
                MEMBER_ORDERS_ADDRESS,
                MEMBER_ORDERS_ADDRESS2,
                MEMBER_ORDERS_POSTCODE,
                MEMBER_ORDERS_DATE,
		        MEMBER_ORDERS_NO,
		        MEMBER_ORDERS_DETAIL_AMOUNT,
		        MEMBER_ORDERS_DETAIL_PRICE,
		        MEMBER_ORDERS_EXD,
		        TRACKING_NUM,
		        MEMBER_ORDERS_DETAIL_STATUS,
                photo_path
        FROM
                RANKEDMOD
        WHERE
                rn=1
        ORDER
        BY
                MEMBER_ORDERS_DATE
        DESC
</select>
	
	
	
	<select id="getProduct" resultType="product">
	    SELECT
		        product_no,
		        product_name,
		        product_price,
		        product_status,
		        product_date,
		        product_photo1,
		        product_photo2,
		        product_photo3,
		        tag_no,
		        product_detail
	    FROM
	        	PRODUCT
	    WHERE
	        	product_no = #{product_no}			
	</select>
	
	


</mapper>